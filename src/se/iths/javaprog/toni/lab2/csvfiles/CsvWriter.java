package se.iths.javaprog.toni.lab2.csvfiles;

public class CsvWriter<T> {






/*
       Files.write()
    monads ShutDownHook class save on exit
    Files.writestring(path,file) write over
    github Files FileDemo append


    public String convertToCSV(String[] data) {
        return Stream.of(data)
            .map(this::formatSpecialCharacters)
            .collect(Collectors.joining(","));
    }*/




//    public void writeToFile(String filePath, List<Product> list) throws IOException {
//        File csvOutputFile = new File(filePath);
//        if(!csvOutputFile.exists())
//            //skapa filen
//        try (PrintWriter pw = new PrintWriter(csvOutputFile)) {
//            list.stream()
//                .map(product-> )
//                .forEach(pw::println);
//        }
//        assertTrue(csvOutputFile.exists());
//    }
}